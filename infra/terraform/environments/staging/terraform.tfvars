# Fill in your values before running terraform apply
subscription_id      = "REPLACE_WITH_YOUR_SUBSCRIPTION_ID"
location             = "eastus"
storage_account_name = "mlopsmonishstg"   # change if name taken — must be globally unique
keyvault_name        = "mlops-monish-kv-stg"  # change if name taken — must be globally unique
